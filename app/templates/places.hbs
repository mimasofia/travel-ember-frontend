{{!-- <h1>These are your Places! Click one for more details!</h1> --}}
{{!-- if (places.length === 0) {
  $('h1').text('To start add a new place')
} else {
  $('h1').text('These are your Places! Click one for more details!')
} --}}
<h1>These are your Places! Click one for more details!</h1>
<div class='places-style'>
{{#each model.places as |place|}}
  <h4> {{#link-to 'place' place}}
    {{place.name}}
    {{/link-to}}
  </h4>
{{/each}}

{{#link-to 'new'}}<button>Add New Place</button>{{/link-to}}
{{outlet}}
</div>
